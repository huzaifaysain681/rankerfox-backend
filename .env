# Server Configuration
PORT=3000                     # Port on which your Node.js app will run

# Puppeteer Configuration
PUPPETEER_HEADLESS=true        # Runs Puppeteer in headless mode (no visible browser UI)
PUPPETEER_ARGS=--no-sandbox,--disable-setuid-sandbox,--disable-dev-shm-usage  
# Puppeteer arguments to improve performance and security in a VPS environment

# CORS Configuration
FRONTEND_ORIGIN=https://frontend.ecom-tools.com/  
# The allowed frontend origin for CORS, make sure it's set to your actual frontend URL

# External Services (e.g., Rankerfox Login URL)
RANKERFOX_LOGIN_URL=https://rankerfox.com/login  
# The URL for your Puppeteer automation to perform login actions on
