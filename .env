# Server Configuration
PORT=3000  # Port on which your Node.js app will run

# Puppeteer Configuration
PUPPETEER_HEADLESS=true  # Runs Puppeteer in headless mode (no visible browser UI)
PUPPETEER_ARGS=--no-sandbox,--disable-setuid-sandbox,--disable-dev-shm-usage  # Puppeteer arguments for performance and security in a VPS environment

# Specify the path to Google Chrome or Chromium (ensure this path is correct)
CHROME_PATH=/usr/bin/google-chrome  # Path to Chrome on your server

# CORS Configuration
FRONTEND_ORIGIN=https://frontend.ecom-tools.com  # The allowed frontend origin for CORS (no trailing slash)

# External Services (e.g., Rankerfox Login URL)
RANKERFOX_LOGIN_URL=https://rankerfox.com/login  # The URL for Puppeteer automation to perform login actions on
